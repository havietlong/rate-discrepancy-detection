{
  "name": "Rate Discrepancy Scanner",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "postCreateCommand": "sudo apt update && sudo apt install -y poppler-utils tesseract-ocr && pip3 install --user -r requirements.txt",
  "postAttachCommand": {
    "server": "streamlit run streamlit_app.py --server.enableCORS false --server.enableXsrfProtection false"
  },
  "forwardPorts": [8501]
}